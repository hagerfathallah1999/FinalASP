namespace FinalASP.Notes
{

    // User Id

    //var claimsId = User.Claims.FirstOrDefault(c => c.Type == ClaimTypes.NameIdentifier);//.FirstOrDefault(c=>c.Type)
    //string id = claimsId.Value;
    //        return View(EmpRepo.GetAll());//view =Index ,Model =List<Employee>


    // Admin Can see only
    //[Authorize(Roles = "Admin")]
}
