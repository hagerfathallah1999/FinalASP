@*


    <div id="carouselExampleControls" class="carousel slide" data-bs-ride="carousel" data-bs-interval="3000" data-bs-pause="hover" data-bs-wrap="true" data-bs-keyboard="true">
        <div class="carousel-inner">

            <a class="explore-our-kitchens-btn>" href="#" >
                    <span>Explore Our Kitchens</span> 
                </a>

            <div class="carousel-item active">
                <img src="~/images/homekitchen01.jpeg" class="d-block w-100" alt="panana">
            </div>
            <div class="carousel-item">
                <img src="~/images/homekitchen02.jpeg" class="d-block w-100" alt="Mango">
            </div>
            <div class="carousel-item">
                <img src="~/images/homekitchen03.jpeg" class="d-block w-100" alt="Oranges">
            </div>
        </div>
    </div>

<style>

        .explore-our-kitchens-btn {
        width: 300px;
        height: 100px;
        background-color: rgba(0, 0, 0, 0.5);
        position: relative;
        left: 25%;
        top: 50%;
    }
    .explore-our-kitchens-btn span {
        color: white;
    }

    img {
        height: 250px;
        width: 100%;
    }

    #carouselExampleControls {
    height: 250px;
    position: absolute;
    top: 135px;
    left:0;
    right:0;
    width :100%;
    }

    .carousel-item {
        transition: transform 7s ease-in-out;
    }

        .carousel-item.next {
            transform: translateX(-100%);
        }

        .carousel-item.prev {
            transform: translateX(100%);
        }
</style>

<script>
    const carousel = new bootstrap.Carousel(document.getElementById('carouselExampleControls'), {
        interval: 3000,
        wrap: true,
        keyboard: true
    });

    carousel._slide = function (carouselItem, slideDirection) {
        const activeItem = this.getActiveCarouselItem();

        if (carouselItem && activeItem && carouselItem !== activeItem) {
            activeItem.classList.remove('active');
            carouselItem.classList.add('active');

            if (slideDirection === 'prev') {
                activeItem.classList.add('prev');
                carouselItem.classList.add('next');
            } else {
                activeItem.classList.add('next');
                carouselItem.classList.add('prev');
            }

            activeItem.addEventListener('transitionend', function () {
                activeItem.classList.remove('prev', 'next');
            }, { once: true });
        }
    };
</script>


*@

    <div id="carouselExampleControls" class="carousel slide" data-bs-ride="carousel" data-bs-interval="3000" data-bs-pause="hover" data-bs-wrap="true" data-bs-keyboard="true">
        <div class="carousel-inner">
            @*        <div class="carousel-item active">
            <img src="~/images/homekitchen01.jpeg" class="d-block w-100" alt="panana">
            </div>
            <div class="carousel-item">
            <img src="~/images/homekitchen02.jpeg" class="d-block w-100" alt="Mango">
            </div>
            <div class="carousel-item">
            <img src="~/images/homekitchen03.jpeg" class="d-block w-100" alt="Oranges">
            </div>*@
            <div class="carousel-item active">
            <img src="~/images/form-function-kitchen-header.jpg" class="d-block w-100" alt="panana">
            </div>
        </div>
        <a class="explore-our-kitchens-btn" href="#">
            <span>Explore Our Kitchens</span>
        </a>
    </div>




<style>
    .explore-our-kitchens-btn {
        width: 300px;
        height: 100px;
        background-color: rgba(0, 0, 0, 0.5);
        position: absolute;
        top: 50%;
        transform: translate(-50%, -50%);
        left: 25%;
        border: 1px;
        border-radius: 20%;
    }

        .explore-our-kitchens-btn span {
            color: white;
            top: 6px;
            position: absolute;
            top: 30px;
            border: -13px;
            left: 13px;
            font-size: 25px;
            font-family: monospace;
            font-weight: bold;
        }

    img {
        height: 463px;
        width: 100%;
    }

    #carouselExampleControls {
        height: 463px;
        position: absolute;
        width: 100%;
        top:130px;
        right:0;
        left:0;
        
    }

    .carousel-item {
        transition: transform 7s ease-in-out;
    }

        .carousel-item.next {
            transform: translateX(-100%);
        }

        .carousel-item.prev {
            transform: translateX(100%);
        }
</style>

<script>
    const carousel = new bootstrap.Carousel(document.getElementById('carouselExampleControls'), {
        interval: 3000,
        wrap: true,
        keyboard: true
    });

    carousel._slide = function (carouselItem, slideDirection) {
        const activeItem = this.getActiveCarouselItem();

        if (carouselItem && activeItem && carouselItem !== activeItem) {
            activeItem.classList.remove('active');
            carouselItem.classList.add('active');

            if (slideDirection === 'prev') {
                activeItem.classList.add('prev');
                carouselItem.classList.add('next');
            } else {
                activeItem.classList.add('next');
                carouselItem.classList.add('prev');
            }

            activeItem.addEventListener('transitionend', function () {
                activeItem.classList.remove('prev', 'next');
            }, { once: true });
        }
    };
</script>